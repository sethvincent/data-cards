var VNode = require('virtual-dom/vnode/vnode');
var VText = require('virtual-dom/vnode/vtext');

var convertHTML = require('./')({
    VNode: VNode,
    VText: VText
});

module.exports = convertHTML;
